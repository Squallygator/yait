# Decision log — specification corpus

> **Generated by `tools/check_specs.py`. Do not hand-edit.** The source of
> truth for any rule is its own `rule.md`; this page is a derived index —
> regenerate it by running the checker again.

60 rules: 47 enforced, 13 assumed exclusions.

| UC | Rule | Group | Polarity | Decided on | Owner | Revisit if |
|---|---|---|---|---|---|---|
| UC-01 | A date a person typed at the head of a filename outranks the camera clock | RG-1.1 Dates written in the filename | Enforced ▶ | 2026-08-27 | squallygator | A device or an export tool appears that writes a leading YYYY-MM-DD prefix mechanically, breaking the "a human typed this" assumption this rule rests on. |
| UC-02 | Dropbox "Camera Uploads" names carry a full timestamp | RG-1.1 Dates written in the filename | Enforced ▶ | 2026-08-27 | squallygator | Dropbox changes its Camera Uploads naming scheme, or another tool starts emitting the same shape from a non-capture time. |
| UC-03 | WhatsApp media names encode the send date | RG-1.1 Dates written in the filename | Enforced ▶ | 2026-08-27 | squallygator | WhatsApp changes its media naming again, or begins preserving the original capture metadata it currently strips. |
| UC-04 | Android camera names embed the capture date | RG-1.1 Dates written in the filename | Enforced ▶ | 2026-08-27 | squallygator | Android or Google Photos introduces a new prefix or changes the date layout (as the Pixel "PXL_" prefix already hints). |
| UC-05 | Screenshot names carry the moment the screenshot was taken | RG-1.1 Dates written in the filename | Enforced ▶ | 2026-08-27 | squallygator | Android changes the screenshot naming scheme, or the iOS / GNOME forms need to be recognised alongside this one. |
| UC-06 | Windows Phone camera names embed the capture date | RG-1.1 Dates written in the filename | Enforced ▶ | 2026-08-27 | squallygator | Never expected to grow. Delete this rule as a unit once no Windows Phone files remain in scope. |
| UC-07 | An isolated YYYYMMDD anywhere in the name is a date | RG-1.1 Dates written in the filename | Enforced ▶ | 2026-08-27 | squallygator | A digit run that is isolated and passes the calendar check is still found in practice not to be a capture date often enough to matter. |
| UC-08 | A long run of digits is an identifier, never mined for a date | RG-1.4 Arbitration and refusals | Assumed exclusion ⊘ | 2026-08-27 | squallygator | A source appears whose long ids embed a genuine, positionally-fixed capture date worth extracting — then this scenario goes red (the date would have to come from "file-name") and an enforced extraction rule replaces this refusal. |
| UC-09 | An epoch timestamp in the name dates the transfer, not the photograph | RG-1.4 Arbitration and refusals | Assumed exclusion ⊘ | 2026-08-27 | squallygator | A workflow appears where the epoch in the name genuinely equals the capture instant — then this scenario goes red (the date would come from "file-name") and an enforced epoch-parsing rule replaces it. |
| UC-10 | A French month name in the filename gives month precision | RG-1.1 Dates written in the filename | Enforced ▶ | 2026-08-27 | squallygator | A collection appears whose language is not French and needs its own month-name table, or where the trailing number turns out to be a day often enough to matter. |
| UC-11 | A numeric YYYY-MM in the filename gives month precision | RG-1.1 Dates written in the filename | Enforced ▶ | 2026-08-27 | squallygator | A source appears where the number after YYYY-MM is reliably the day of month rather than a counter. |
| UC-12 | A year on its own in the filename gives year precision | RG-1.1 Dates written in the filename | Enforced ▶ | 2026-08-27 | squallygator | Four-digit camera sequence numbers in the plausible year range (IMG_2011.jpg) cause enough false year matches to need a guard. |
| UC-13 | Which written forms count as a folder date | RG-1.2 Dates carried by folder names | Enforced ▶ | 2026-08-27 | squallygator | A collection appears that is US-formatted (month-first), which would need an explicit per-collection convention rather than a global one. |
| UC-14 | The deepest dated folder wins over its ancestors | RG-1.2 Dates carried by folder names | Enforced ▶ | 2026-08-24 | squallygator | An archive appears where inner folders are dated by processing date rather than capture date, making the outer folder the truthful one. |
| UC-15 | A French month name in a folder gives month precision | RG-1.2 Dates carried by folder names | Enforced ▶ | 2026-08-27 | squallygator | A collection appears where the number beside a spelled-out month is consistently the day of month. |
| UC-16 | An unpadded digit after the year is a disc number, not a month | RG-1.2 Dates carried by folder names | Assumed exclusion ⊘ | 2026-08-27 | squallygator | An archive appears that uses unpadded "YYYY-N" folders to mean real months. That would turn the scenario below red: the folder would have to resolve to "2006-02" at month precision, and this rule folder would be deleted in favour of an enforced YYYY-N month rule. |
| UC-17 | A still's capture date comes from Exif, DateTimeOriginal first | RG-1.3 Dates read from the file's content | Enforced ▶ | 2026-08-27 | squallygator | A camera or workflow is found that populates DateTimeOriginal with something other than the capture instant, making the order wrong. |
| UC-18 | A still with no Exif date is normal, not a dead end | RG-1.3 Dates read from the file's content | Enforced ▶ | 2026-08-27 | squallygator | A case appears where continuing past absent metadata produces a worse answer than stopping would. |
| UC-19 | Embedded date far from the folder date is treated as clock drift | RG-1.4 Arbitration and refusals | Enforced ▶ | 2026-08-27 | squallygator | An archive appears whose folders are dated by processing date, so a far-apart embedded date is the correct one and this guard inverts the right answer. |
| UC-20 | An AVI's capture date comes from its IDIT chunk | RG-1.3 Dates read from the file's content | Enforced ▶ | 2026-08-27 | squallygator | A camera family is found that writes IDIT in a non-asctime format, needing a second parser. |
| UC-21 | An MP4/MOV's capture date comes from the moov/mvhd atom | RG-1.3 Dates read from the file's content | Enforced ▶ | 2026-08-27 | squallygator | A camera is found that leaves mvhd at 0 but fills a usable date elsewhere in the container (e.g. ©day), warranting a fallback. |
| UC-22 | A .THM thumbnail dates its video | RG-1.3 Dates read from the file's content | Enforced ▶ | 2026-08-27 | squallygator | A sidecar type appears whose date should *not* propagate to its media (e.g. an edit-side thumbnail regenerated years later). |
| UC-23 | An archive takes the oldest date it contains | RG-1.3 Dates read from the file's content | Enforced ▶ | 2026-08-27 | squallygator | A workflow appears where archives are incremental and the newest entry is the meaningful date. |
| UC-24 | A file that cannot be read is reported, never silently skipped | RG-2 File classification | Enforced ▶ | 2026-09-09 | squallygator | A partial-recovery path appears — reading the intact leading portion of a truncated JPEG to salvage its Exif — at which point "unreadable" would gain a sub-state for "readable enough to date". |
| UC-25 | A zero-byte file is a failed copy, not a photograph | RG-2 File classification | Enforced ▶ | 2026-09-09 | squallygator | A workflow appears that deliberately uses zero-byte files as markers the tool should preserve in place rather than set aside. |
| UC-26 | Photo-manager and operating-system leftovers are junk | RG-2 File classification | Enforced ▶ | 2026-09-09 | squallygator | A leftover type appears that carries information worth keeping (as the .THM does), in which case it earns its own rule rather than a place on this list. |
| UC-27 | The higher-quality file keeps the clean name | RG-4 Collisions | Enforced ▶ | 2026-09-09 | squallygator | A collection appears where the largest file is routinely the worst (upscaled exports), making size or a real quality metric the better lead key. |
| UC-28 | A camera master in an "Originals" folder takes an -original suffix | RG-3 Target naming | Enforced ▶ | 2026-09-09 | squallygator | A collection uses "Originals" to mean something else (an album title, a scanner's output folder) often enough that the suffix becomes misleading. |
| UC-29 | When nothing answers, the file is undated, and that is a real state | RG-1.4 Arbitration and refusals | Enforced ▶ | 2026-08-27 | squallygator | A new source of truth is added (e.g. an external index) that could answer for files currently ending here. |
| UC-30 | A candidate date outside the plausible year range is rejected | RG-1.4 Arbitration and refusals | Enforced ▶ | 2026-08-27 | squallygator | Media older than 1970 (scanned 1960s prints) enters scope, or a reason appears to allow a small amount of future slack for timezone skew. |
| UC-31 | An unrecognised filename prefix is kept, not stripped | RG-5 Metadata deduction and writing | Enforced ▶ | 2026-09-09 | squallygator | Unlisted-but-real camera prefixes turn out to be so common in practice that a careful shape heuristic beats the list's coverage. |
| UC-32 | The label is deduced from the folder and the filename together | RG-5 Metadata deduction and writing | Enforced ▶ | 2026-09-09 | squallygator | Folder-label noise (Originals, Scans, New folder) turns out to dominate real collections, making a stop-list part of this rule rather than a later refinement. |
| UC-33 | A caption field a human already filled is never overwritten | RG-5 Metadata deduction and writing | Assumed exclusion ⊘ | 2026-09-09 | squallygator | A reliable way appears to tell a machine-dumped placeholder (a bare camera filename in the title) from a real human caption — then those placeholders could be replaced, this scenario would go red for that case, and an enforced "replace placeholder" rule would take over. |
| UC-34 | Paths beyond 260 characters still work | RG-7 Safety and reversibility | Enforced ▶ | 2026-09-09 | squallygator | Windows' long-path opt-in becomes universal and unconditional, making the explicit \\?\ handling redundant (it would stay harmless). |
| UC-35 | Accents, ampersands and parentheses are kept in the target name | RG-3 Target naming | Enforced ▶ | 2026-09-09 | squallygator | A downstream consumer appears that genuinely cannot handle non-ASCII filenames (an old sync client, a FAT device), in which case transliteration becomes an opt-in output mode, not the default. |
| UC-36 | The full source priority order | RG-1.4 Arbitration and refusals | Enforced ▶ | 2026-08-27 | squallygator | A new source is introduced (an external index, a trusted sidecar type) and has to be placed in the ladder, or field experience shows two adjacent ranks are in the wrong order. |
| UC-37 | The filesystem modification time is never a capture date | RG-1.4 Arbitration and refusals | Assumed exclusion ⊘ | 2026-08-27 | squallygator | A workflow guarantees an un-copied tree with trustworthy birth times — even then, a per-collection opt-in would be safer than reversing this globally, so the scenario below stays the guard. |
| UC-38 | A .THM sidecar survives as long as its video does | RG-2 File classification | Enforced ▶ | 2026-09-09 | squallygator | A camera family appears whose .THM is a regenerated edit-side preview with no capture date, making it genuinely disposable. |
| UC-39 | A date token is dropped from the description only if it supplied the date | RG-3 Target naming | Enforced ▶ | 2026-09-09 | squallygator | A resolver change stops reporting the exact source span, making "the token that was used" impossible to identify precisely. |
| UC-40 | Characters Windows refuses are removed from the target name | RG-3 Target naming | Enforced ▶ | 2026-09-09 | squallygator | The tool ever targets a filesystem with a different forbidden set (it would widen, not narrow — POSIX forbids only "/" and NUL). |
| UC-41 | Running the namer twice changes nothing the second time | RG-3 Target naming | Enforced ▶ | 2026-09-09 | squallygator | The name format changes shape (a new prefix or suffix), which would require the re-run to recognise both the old and the new form as "already done" during the transition. |
| UC-42 | Target-name uniqueness is checked across the whole collection | RG-4 Collisions | Enforced ▶ | 2026-09-09 | squallygator | A layout is added that keeps source folders permanently and never flattens, making per-folder uniqueness sufficient for that mode. |
| UC-43 | One deduced label is written to title, subject and comment | RG-5 Metadata deduction and writing | Enforced ▶ | 2026-09-09 | squallygator | A metadata standard consolidates on one caption field widely enough that writing three becomes redundant clutter. |
| UC-44 | Writing metadata never re-encodes the image | RG-5 Metadata deduction and writing | Enforced ▶ | 2026-09-09 | squallygator | Never for JPEG. A future format that is cheap to fully re-serialise losslessly would not need the split-and-splice, but would still not be *re-encoded*. |
| UC-45 | A file that changed since the audit is not written blind | RG-5 Metadata deduction and writing | Enforced ▶ | 2026-09-09 | squallygator | Fingerprinting every file at apply time becomes a measurable cost on very large collections, forcing a cheaper (mtime+size) fast path with content hash only on mismatch. |
| UC-46 | Placement follows the precision of the resolved date | RG-6 Organizing | Enforced ▶ | 2026-09-09 | squallygator | Users ask for a flat YYYY/ tree regardless of precision — that is a layout option, not a change to this rule. |
| UC-47 | Flatten leaves an undated file where it is | RG-6 Organizing | Assumed exclusion ⊘ | 2026-09-09 | squallygator | A flatten consumer appears that can carry folder context some other way (a manifest, a tag), making it safe to move undated files in too — then this scenario goes red (the file would no longer be at its original path) and an enforced rule replaces it. |
| UC-48 | A source folder left empty by the move is pruned, and only then counted | RG-6 Organizing | Enforced ▶ | 2026-09-09 | squallygator | Users want the original folder structure kept as an archive alongside the new tree, making pruning an opt-out. |
| UC-49 | An interrupted write never truncates the original | RG-7 Safety and reversibility | Enforced ▶ | 2026-09-09 | squallygator | Never. This is a data-safety non-negotiable (CLAUDE.md). |
| UC-50 | The journal entry is written and flushed before the operation runs | RG-7 Safety and reversibility | Enforced ▶ | 2026-09-09 | squallygator | Never. This is a data-safety non-negotiable (CLAUDE.md). |
| UC-51 | Undo restores the file bit for bit, not approximately | RG-7 Safety and reversibility | Enforced ▶ | 2026-09-09 | squallygator | Never. Reversibility is a data-safety non-negotiable (CLAUDE.md). |
| UC-52 | A path from outside cannot escape the collection root | RG-7 Safety and reversibility | Enforced ▶ | 2026-09-09 | squallygator | Never. Path confinement is a data-safety non-negotiable (CLAUDE.md); it only ever tightens. |
| UC-53 | An occupied destination is an error, never a silent suffix | RG-7 Safety and reversibility | Enforced ▶ | 2026-09-09 | squallygator | Never. "Never overwrite" is a data-safety non-negotiable (CLAUDE.md). |
| UC-54 | RAW files are not opened to read their capture date | RG-1.3 Dates read from the file's content | Assumed exclusion ⊘ | 2026-08-27 | squallygator | A collection appears with RAW files in undated folders and uninformative names, making the embedded date the only source — at which point an ADR for a RAW reader is on the table, this scenario goes red, and the rule folder is replaced by an enforced RAW rule. |
| UC-55 | HEIC/HEIF files are not opened to read their capture date | RG-1.3 Dates read from the file's content | Assumed exclusion ⊘ | 2026-08-27 | squallygator | HEIC starts arriving in undated folders with uninformative names, or Pillow gains built-in HEIC support so the dependency cost disappears — either turns this scenario red and replaces the rule. |
| UC-56 | Google Takeout companion JSON is not read for a date | RG-1.4 Arbitration and refusals | Assumed exclusion ⊘ | 2026-08-27 | squallygator | Google fixes photoTakenTime reliability, or a collection has media with no other date where the JSON is the only witness — then this scenario goes red (the date would come from "sidecar") and an enforced JSON rule replaces it. |
| UC-57 | GPS metadata is never used to resolve a capture date | RG-1.4 Arbitration and refusals | Assumed exclusion ⊘ | 2026-08-27 | squallygator | A camera or workflow is found where GPSDateStamp is reliably the capture instant and is the only date present often enough to matter — then this scenario goes red and an enforced GPS-date rule replaces it. |
| UC-58 | Archives are read one level deep, not recursed | RG-1.3 Dates read from the file's content | Assumed exclusion ⊘ | 2026-08-27 | squallygator | A real collection is found whose meaningful dates live only inside nested archives — then this scenario goes red and a bounded-depth recursion rule replaces it. |
| UC-59 | Identical content in two files is not detected or merged | RG-2 File classification | Assumed exclusion ⊘ | 2026-09-09 | squallygator | A collection appears that is mostly exact duplicates from repeated backups, where keeping both copies is pure waste — then a dedup step gets its own design, this scenario goes red (one file would have no target name of its own), and an enforced rule replaces this one. |
| UC-60 | Timestamps are kept as written; no timezone is applied | RG-1.4 Arbitration and refusals | Assumed exclusion ⊘ | 2026-08-27 | squallygator | A use case appears that genuinely needs UTC-normalised dates — even then a display-time toggle would be safer than changing what "resolved date" means, so this scenario stays the guard. |

