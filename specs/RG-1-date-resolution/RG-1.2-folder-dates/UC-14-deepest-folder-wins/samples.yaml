# UC-14 — the deepest dated folder wins.
#
# One image, deliberately stripped of every metadata field: the path must be the
# only thing that can answer. The two dated ancestors disagree, and the nearer
# one is EARLIER than the outer one, so an implementation that picks the maximum,
# the minimum, or the first match while walking down cannot pass by accident.

version: 1

files:
  - path: "2002-07-20 Wedding at Arras/18-07-2002/026-the-couple-and-the-mother.jpg"
    source: seed
    width: 320
    exif: {}
