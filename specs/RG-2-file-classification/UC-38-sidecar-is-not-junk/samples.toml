# UC-38 — a matched .THM is a sidecar, not junk (twin of UC-22 in RG-1.3).
#
# A forged AVI and, beside it, a seed-derived JPEG thumbnail sharing its stem.
# The thumbnail carries a capture date (a real .THM always does), but this rule
# only asserts its classification; the dating is UC-22's job. A different stem
# from UC-22's own sample keeps the two rules visually distinct.

version = 1

[[files]]
path = "CANON/MVI_3312.AVI"
source = "forge"
kind = "riff-idit"
params = { idit = "unknown" }

[[files]]
path = "CANON/MVI_3312.THM"
source = "seed"
width = 160
exif = { DateTimeOriginal = "2006-07-14 18:30:00" }
